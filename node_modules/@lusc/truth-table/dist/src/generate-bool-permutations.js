// Use array because it's easier with recursion
function* generateBoolPermutationsIterator(variables, offset = 0, accumulator = {}) {
    const variable0 = variables[offset];
    ++offset;
    if (variable0) {
        yield* generateBoolPermutationsIterator(variables, offset, {
            ...accumulator,
            [variable0]: true,
        });
        yield* generateBoolPermutationsIterator(variables, offset, {
            ...accumulator,
            [variable0]: false,
        });
    }
    else {
        yield accumulator;
    }
}
// Use Set to not have duplicates
export const generateBoolPermutations = (variables) => generateBoolPermutationsIterator([...variables]);
//# sourceMappingURL=generate-bool-permutations.js.map