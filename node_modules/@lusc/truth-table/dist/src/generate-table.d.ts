import type { Except, ReadonlyDeep } from 'type-fest';
import { type AST } from './parse-operation.js';
type MutableTable = {
    columns: string[];
    rows: boolean[][];
    ast: AST;
};
type TableReadonlyKeys = 'columns' | 'rows';
export type ParsedTable = ReadonlyDeep<Pick<MutableTable, TableReadonlyKeys>> & Readonly<Except<MutableTable, TableReadonlyKeys>>;
export declare const generateTable: (input: string, includeSteps?: boolean) => ParsedTable;
export {};
