import { Operator } from '../operators.js';
import { TokenType } from '../tokenize.js';
import { IndexedError } from '../indexed-error.js';
export const validateNots = (input) => {
    let lastType;
    for (const item of input) {
        if (item.type === TokenType.operator
            && lastType === TokenType.operator
            && item.operator !== Operator.not) {
            throw new IndexedError(`Unexpected operator "${item.source.slice(item.from, item.to)}".`, item.from, item.to);
        }
        lastType = item.type;
    }
};
//# sourceMappingURL=validate-nots.js.map