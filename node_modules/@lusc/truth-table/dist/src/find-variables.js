function* findVariablesRecursive(operation) {
    if (operation.type === 'variable') {
        yield operation.variable;
    }
    else {
        for (const value of operation.values) {
            yield* findVariablesRecursive(value);
        }
    }
}
export const findVariables = (operation) => new Set(findVariablesRecursive(operation));
//# sourceMappingURL=find-variables.js.map