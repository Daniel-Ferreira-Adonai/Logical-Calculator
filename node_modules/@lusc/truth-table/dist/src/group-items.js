import { TokenType } from './tokenize.js';
export const groupItems = (input) => {
    let previous = [];
    const result = [];
    let depth = 0;
    const pushResult = () => {
        if (previous.length > 0 && depth === 0) {
            result.push(previous);
            previous = [];
        }
    };
    for (const item of input) {
        if (item.type === TokenType.parens) {
            pushResult();
            previous.push(item);
            if (item.parensType === 'open') {
                ++depth;
            }
            else {
                --depth;
            }
            pushResult();
        }
        else if (depth === 0) {
            result.push([item]);
        }
        else {
            previous.push(item);
        }
    }
    pushResult();
    return result;
};
//# sourceMappingURL=group-items.js.map